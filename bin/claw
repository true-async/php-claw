#!/usr/bin/env php
<?php

declare(strict_types=1);

use Claw\Cli\Cli;

require __DIR__ . '/../vendor/autoload.php';

$argv = array_values(array_map(strval(...), $_SERVER['argv'] ?? []));

exit((new Cli(\dirname(__DIR__)))->run($argv));
